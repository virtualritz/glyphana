name: CI

on:
  push:
    branches: [master]
  pull_request:

permissions:
  contents: read

env:
  CARGO_TERM_COLOR: always

jobs:
  check:
    name: fmt, clippy, test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # The repo pins nightly via rust-toolchain.toml, so install the
      # components on nightly (otherwise cargo fmt/clippy fail: the pinned
      # toolchain has no rustfmt/clippy). Nothing actually requires nightly --
      # switching rust-toolchain.toml to stable would make this more robust.
      - name: Setup Rust
        uses: dtolnay/rust-toolchain@nightly
        with:
          components: rustfmt, clippy

      - name: Setup Rust cache
        uses: Swatinem/rust-cache@v2

      # Build-time deps for the GTK tray (gtk3/appindicator) and windowing
      # (xkbcommon/wayland/xdo); pkg-config locates them. TLS is pure-Rust
      # (rustls/ring via the google-fonts fork), so no libssl is needed.
      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libgtk-3-dev \
            libxdo-dev \
            libxkbcommon-dev \
            libwayland-dev \
            libayatana-appindicator3-dev \
            pkg-config

      - name: Check formatting
        run: cargo fmt --all -- --check

      - name: Clippy
        run: cargo clippy --all-features --all-targets -- -D warnings

      - name: Test
        run: cargo test --all-features
